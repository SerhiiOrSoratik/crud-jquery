export type NewOrUpdatedEntity = {
    caption: string,
    price: number,
    description: string,
}

export type Entity = {
    id: string,
    caption: string,
    price: number,
    description: string,
    availability: boolean
}
